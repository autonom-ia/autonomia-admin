# Session Audit Log — deploy-gate do Autonom.ia Admin

- Data: 2026-07-13 13:02 WEST
- Issue: #8
- Branch: `fix/8-production-deploy-gate`
- Base: `docs/update-agent-harness-v1` / Draft PR #10
- PR: não aberta
- Ambiente: Local/CI; produção não alterada
- Modelo: strong + human

## Evidência inicial

- push em `main` executava deploy produtivo;
- o mesmo job invocava a Lambda de migrations após publicar código;
- Serverless assumia stage `prod` quando `--stage` não era informado;
- `package.json` expunha script genérico `deploy`;
- migrations incluem alterações e updates sobre dados existentes;
- Environment produtivo e branch não tinham proteções certificadas.

## Decisão

Hard-disable do workflow GitHub, remoção do default `prod` e do script genérico.
Esta PR é uma fase parcial (`Refs #8`) e não cria uma release nova nem encerra
a Issue: a reativação deve provar banco AppSell
isolado, migrations aditivas, staging, artifact por SHA, smoke e rollback.
O package local usa `DATABASE_URL` dummy e stage `ci`, sem ler SSM produtivo.

## Restrições

Nenhum deploy, migration, secret, banco, AWS, Environment ou dado de cliente.
Não fazer merge.

## Validação

- `pnpm test:deploy-gate`: verde, incluindo 9 mutações negativas;
- lint e build: verdes;
- Vitest: 3/3 testes ignorados sem banco; não prova runtime;
- package stage `ci` com `DATABASE_URL` dummy: verde;
- Harness 2.1.2 e Doctor: 0 erros/0 warnings;
- YAML dos workflows/serverless e diff check: verdes;
- primeiro review crítico RED P1=2/P2=1: checker não cobria workflows/scripts
  alternativos, `prod` aceitava DB por env e docs legadas contradiziam o gate;
- checker ampliado para todos os workflows/scripts e stage real, DB por allowlist
  (`ci` env; `prod` SSM exclusivo) e docs alinhadas como fase parcial;
- resolver de DB separado por arquivo de stage; package `ci` não resolve SSM
  produtivo e qualquer stage não allowlisted falha por arquivo inexistente;
- segundo review RED P1: denylist ainda aceitava actions/comandos/scripts
  indiretos; substituída por allowlist de nomes + SHA-256 dos três workflows e
  allowlist exata de todos os scripts do `package.json`;
- lint/build verdes; package `ci` dummy verde; Vitest 3/3 ignorados;
- novo review crítico: pendente.

## Próxima ação

Executar validação completa e submeter o hash staged a review crítico.
