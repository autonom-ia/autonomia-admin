# Session Audit — JWT fail-closed e RBAC global

Data: 2026-07-13
Issue: #3, dependências #2 e #8
Branch: `feat/3-platform-rbac`
PR: Draft #12
Ambiente: Local/CI
Modelo: strong + human

## Decisões

- Consolidar o auth fail-closed da Draft PR #6 sobre a pilha de deploy-gate da
  Draft PR #11, preservando o hard-disable produtivo.
- Manter a migration cross-project 008 fora do runner Local/CI.
- Resolver permissão somente por `JWT sub → admin.users → user_roles → roles`.
- Não usar profile, membership ou email como autoridade global.
- Reservar o email `comercial@autonomia.site`, mas exigir o `sub` exato
  configurado e ID token assinado/verificado no bootstrap one-shot.
- Fazer autenticação read-only em rotas protegidas; primeiro vínculo e
  bootstrap só podem escrever durante `GET /admin/me` bem-sucedido.

## Implementação local

- migration 012 aditiva com role `platform_superadmin` e singleton auditável;
- bootstrap desabilitado por padrão, transacional e serializado por advisory lock;
- permissões persistidas e `requirePermission` por família de rotas;
- requests sem role recebem 403 antes de Zod, repository, SQS ou S3;
- CI ajustada para PostgreSQL 16 descartável no contrato exato do guard local;
- testes de bootstrap correto/incorreto, retry, email alterado, usuário comum,
  route matrix, migration idempotente e role divergente fail-closed.

## Validação até este ponto

- `pnpm lint`: verde;
- `pnpm migrate:local`: 10 migrations Admin-only, incluindo 012; 008 excluída;
- `pnpm test`: 55/55, zero skipped, PostgreSQL local real e JWKS RSA efêmero, incluindo enforcement direto e concorrente do trigger de proteção do superadmin;
- deploy-gate com closure atualizado e fixtures negativas: verde;
- build, package ci, audit de produção e Harness 2.1.2: verdes;
- convite conflitante, alteração, desativação e soft-delete do usuário
  bootstrapado retornam 409; Admin/Auth permanecem ativos;
- nenhuma VPS, AWS, RDS, secret, produção ou dado de cliente acessado.

## Pendente

- CI remoto da Draft PR #12;
- aprovação humana posterior; sem merge/deploy nesta fase.

## Review independente

- GREEN final: P0=0, P1=0, P2=0, P3=0;
- confirmou trigger direto e corrida TOCTOU, JWT fail-closed, bootstrap
  serializado, RBAC persistido, guards 409 e rollback não destrutivo.
